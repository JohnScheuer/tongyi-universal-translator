# TōngYì Translator (通译)

Windows system-tray app (Rust) that captures text from the active field via hotkey, translates PT/EN/ES → Chinese Simplified, and replaces the text via clipboard.

## Status
Bootstrap (Step 1): prints version.

## Build
For now:
- `cargo run`
